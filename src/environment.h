#pragma once

// Variable storage and call stack for the rocBAS interpreter.
// Will be populated in Step 6.

namespace rocbas {

class Environment {
public:
    Environment() = default;
};

} // namespace rocbas
