10 MPI INIT
20 LET R = MPI RANK
30 DIM A(1)
40 IF R = 1 THEN GOTO 80
50 LET A(1) = 100
60 MPI SEND A TO 1 TAG 1
70 MPI RECV A FROM 1 TAG 2
75 PRINT A(1)
76 GOTO 110
80 MPI RECV A FROM 0 TAG 1
90 LET A(1) = A(1) * 2
100 MPI SEND A TO 0 TAG 2
110 MPI FINALIZE
120 END
