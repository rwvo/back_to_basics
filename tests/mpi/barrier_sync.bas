10 MPI INIT
20 LET R = MPI RANK
30 DIM A(1)
40 IF R = 1 THEN GOTO 80
50 LET A(1) = 100
60 MPI BARRIER
70 MPI SEND A TO 1 TAG 1
75 GOTO 110
80 MPI BARRIER
90 MPI RECV A FROM 0 TAG 1
100 PRINT A(1)
110 MPI FINALIZE
120 END
