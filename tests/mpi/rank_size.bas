10 MPI INIT
20 LET R = MPI RANK
30 LET S = MPI SIZE
40 DIM A(1), B(1)
50 LET A(1) = R * 10 + S
60 IF R = 0 THEN GOTO 90
70 MPI SEND A TO 0 TAG 1
80 GOTO 120
90 MPI RECV B FROM 1 TAG 1
100 PRINT A(1)
110 PRINT B(1)
120 MPI FINALIZE
130 END
